<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--免入侵获取Content-->
        <provider
            android:name=".global.LibInitProvider"
            android:authorities="${applicationId}.init"
            android:exported="false" />
    </application>

</manifest>