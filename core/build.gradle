plugins {
    alias(libs.plugins.android.library)
    alias(libs.plugins.kotlin.android)
}

android {
    namespace 'com.angcyo.compose.core'
}

apply from: allGradle.composeLib

dependencies {
    implementation project(allModule.basics)
}